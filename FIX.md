# FIXED BUILD
This package replaces the previous broken package.

Critical fixes:
- Corrected Java string escaping in MediaNotificationListener.java.
- Added missing desktop/server/server.py required by desktop/start.bat.
- Android Java compilation is explicitly set to Java 17.

Upload/replace the repository contents with this package, then run:
Actions -> Build Phone YTM Companion APK -> Run workflow

Do not mix files from the previous version with this one.
